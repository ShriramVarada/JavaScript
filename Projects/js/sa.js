var docd = document.documentElement;
docd.className = docd.className.replace(/(^|\s+)no-js(\s+|$)/, '1');

// Accordion (pg 502), Tab panel (pg 506)

// Modals

var modal = {
  center: function(){

  };

  open :function(){

  };

  close: function(){

  };
};

// Photo viewer, Slider (pg 530)
